# Metadata for data/processed/rotation.csv — required for honest dataset labeling.
# This file describes the CURRENT demo fixture (2 galaxies), not real SPARC.

dataset_type: demo_fixture
source: test_fixture
description: "Small demo dataset used to test pipeline behavior."
is_real_observational_data: false

# To run as real observational calibration, replace rotation.csv with your
# processed SPARC (or other) table and set:
#
# dataset_type: real_observational
# source: SPARC
# description: "Processed SPARC rotation curves."
# is_real_observational_data: true
